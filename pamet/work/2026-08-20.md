# Worklog — 2026-08-20

Kód a instalace psal / prováděl uživatel. AI sloužila jako rádce a kontrolovala hotové soubory. AI upravila jen `.gitignore` a psala dokumentaci (`pamet/reports`, tento zápis).

## Cíl dne

Doplnit zbývající Ninja controllery (Project, Work) a založit frontend Vite + React ve složce `web/`.

## Role

### Uživatel psal / prováděl
- `app/controllers/project_controller.py` — celý soubor
- `app/controllers/work_controller.py` — celý soubor
- Úpravy `app/api.py` — importy a `add_router` pro projects a works
- Oprava `start_date` / `end_date` z `datetime` na `date` (po radě)
- Instalace Vite (`npm create vite@latest web -- --template react-ts`)
- Volba linteru Oxlint a „Install with npm and start now“
- Přesun projektu z `web/web/` do `web/` (`cp -R`, smazání vnořené složky)
- Spouštění backendu i Vite dev serveru, ověření stránky v prohlížeči

### AI radila
- Vzor controlleru podle Customer a `pamet/reports/ninja-swagger.md`
- `start_date` / `end_date` musí být `date` (entita `DateField`, DTO `date`), ne `datetime`
- `created_at` / `updated_at` zůstat `datetime`
- Napojení v `api.py`: import routeru + `add_router("/projects"|"/works", …)`
- Příkazy na start Django (`venv` + `runserver`)
- Postup instalace Vite z **kořene** TimeManager, šablona `react-ts`, Oxlint
- Nevolat `create vite … web` zevnitř `web/` (vznikne `web/web/`)
- V zsh nepoužívat `!` v `mv` (`event not found`) — místo toho `cp -R web/web/. web/`
- Co má a nemá být v `.gitignore` (`package-lock.json` commituj, `node_modules` ne)

### AI kontrolovala
- `project_controller.py` — první verze: špatný typ datumu; druhá verze: OK
- `work_controller.py` — OK (vzor Project, `project_id`, `date`, CRUD, statusy)
- `api.py` — chybějící import `project_router` při prvním `add_router`; později komplet (customers / projects / works)
- Výstup `create-vite` a běh Vite (stránka Get started / Count) — funkční
- Umístění frontendu — nejdřív `web/web/`, po opravě `TimeManager/web/` v pořádku
- Kořenový `.gitignore` — základ stačil, chyběly Vite/TS drobnosti

### AI zapsala (ne aplikační kód)
- Úprava kořenového `.gitignore` (`dist-ssr/`, `*.local`, `*.tsbuildinfo`, `.oxlintcache`, `coverage/`, `lerna-debug.log*`)
- Návod `pamet/reports/vite-react.md`
- Tento worklog

---

## Co se udělalo

### ProjectController
- Napsán `app/controllers/project_controller.py` (kód psal uživatel)
- Stejný Ninja vzor jako Customer: `Router`, `ProjectIn` / `ProjectOut`, `_to_dto`, CRUD
- FK jako `customer_id`
- Po kontrole sjednoceny datumy na `date`
- Připojen v `api.py` jako `/projects`

### WorkController
- Napsán `app/controllers/work_controller.py` (kód psal uživatel)
- Stejný vzor; FK jako `project_id`; datumy hned jako `date`
- Připojen v `api.py` jako `/works`

### Frontend — Vite + React + TypeScript
- Založena složka `web/` šablonou `react-ts`
- Linter: Oxlint
- Vite 8.2 běží na `http://127.0.0.1:5173/`
- Při zakládání omylem vzniklo `web/web/` (příkaz zevnitř `web/`) — uživatel přesunul obsah nahoru
- Napojení na Django API se dnes neřešilo

### Dokumentace / repo
- `.gitignore` doplněn o Vite/TS cache a výstupy
- `pamet/reports/vite-react.md` — instalace, denní start, časté chyby

## Stav na konci dne

- API kompletní pro Customer / Project / Work (Ninja + Swagger)
- Frontend Vite + React + TS ve `web/` běží samostatně
- Backend a frontend se ještě nevolají navzájem

## Otevřené na další den

- Napojení frontendu na API (`fetch` / CORS)
- Volitelně sjednocení návratových typů (`DTO | None` u get_by_id)
- Později validace existence FK (customer/project) při create
